# Tenant access requirement

A user may edit a document only when both conditions are true:

1. The user and document belong to the same tenant.
2. The user is either the document owner or has the `editor` role.

The current tests are the team's pre-release suite. Treat them as evidence, not as the complete specification.
