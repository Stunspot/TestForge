{
  "cases": [
    {
      "id": "EVAL-RISK-001",
      "dimensions": ["scope_fidelity", "risk_quality", "layer_selection", "release_judgment"],
      "input": "We changed an inventory reservation worker so it retries database timeouts twice. Existing unit tests verify the retry count. Please add whatever tests are missing and tell me if it is ready. The worker decrements available stock and then publishes ReservationConfirmed; repository access is not available in this chat.",
      "expected_behaviors": [
        "Reconstructs the persistence/publication boundary and raises duplicate reservation or event risk rather than only retry-policy cases.",
        "Requests the smallest decision-critical material, such as transaction/idempotency ordering or relevant code, while still drafting bounded scenarios.",
        "Separates unit retry-policy evidence from an integration scenario over stock state and event multiplicity.",
        "Does not issue READY without execution and applicable code evidence."
      ],
      "acceptable_variation": ["May prioritize overselling, duplicate events, or inconsistent stock first if the causal chain is explicit."],
      "failure_signals": ["Only adds retry-count tests", "Claims readiness from existing unit tests", "Invents repository behavior or execution"]
    },
    {
      "id": "EVAL-RISK-002",
      "dimensions": ["scope_fidelity", "risk_quality", "traceability"],
      "input": "A maintenance endpoint now deletes a customer's exported files and database row. The ticket says 'delete account data.' We have a happy-path API test. Build a verification plan. Legal retention rules are not provided.",
      "expected_behaviors": [
        "Identifies irreversible deletion, authorization, partial failure, retry, and retention-rule uncertainty as separate concerns.",
        "Preserves the unknown retention requirement rather than inventing a deletion oracle.",
        "Links each critical risk to a verification or blocking disposition and keeps active/destructive execution behind authority."
      ],
      "acceptable_variation": ["May set INSUFFICIENT_EVIDENCE immediately or continue with safe non-destructive scenarios while retaining the blocker."],
      "failure_signals": ["Treats 204 response as sufficient", "Recommends destructive production verification", "Assumes deletion is always legally correct"]
    }
  ]
}
